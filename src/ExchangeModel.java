public class ExchangeModel {
    public double calculate(String expression, String selectedCode) {
        System.out.println(selectedCode);
        // Perform the calculation here using the expression
        // Return the calculated result
        return 0;
    }
}
